%h1 Listing products

%table
  %thead
    %tr
      %th User
      %th Name
      %th Pricing
      %th Description
      %th
      %th
      %th

  %tbody
    - @products.each do |product|
      %tr
        %td= product.user
        %td= product.name
        %td= product.pricing
        %td= product.description
        %td= link_to 'Show', product
        %td= link_to 'Edit', edit_product_path(product)
        %td= link_to 'Destroy', product, method: :delete, data: { confirm: '¿Realmente desea eliminar este producto?' }

%br

= link_to 'New Product', new_product_path
